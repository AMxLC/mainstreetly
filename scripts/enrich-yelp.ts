/**
 * enrich-yelp.ts — Enrich businesses with Yelp Fusion API data
 *
 * Prerequisites:
 * 1. Sign up for Yelp Fusion API (30-day trial, 5,000 free calls)
 * 2. Set YELP_API_KEY in your .env file
 *
 * This script will:
 * - Iterate through businesses in the database
 * - Search Yelp API by name + location
 * - Match and update with Yelp rating, review count, yelp_id
 * - Recalculate composite rating
 */

console.log("Yelp enrichment script — not yet implemented.");
console.log("Set YELP_API_KEY in .env and rerun.");
process.exit(0);
